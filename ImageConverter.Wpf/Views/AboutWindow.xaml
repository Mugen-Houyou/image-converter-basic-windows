<Window x:Class="ImageConverter.Wpf.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About 이미지 변환기"
        Width="380" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="이미지 변환기" FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" FontSize="13" Margin="0,0,0,4">
            <Run Text="Copyright © " />
            <Run Text="Mugen-Houyou" FontWeight="Bold" />
            <Run Text=", 2026-" />
            <Run x:Name="CurrentYear" />
            <Run Text=". All rights reserved." />
        </TextBlock>

        <TextBlock Grid.Row="2" FontSize="13" Margin="0,0,0,12"
                   TextWrapping="Wrap">
            <Run Text="This project is distributed under the " />
            <Run Text="GNU Affero General Public License v3.0" FontWeight="SemiBold" />
            <Run Text=" (AGPLv3)." />
        </TextBlock>

        <Button Grid.Row="4" Content="확인" Width="80" HorizontalAlignment="Right"
                IsCancel="True" Click="Close_Click" />
    </Grid>
</Window>
